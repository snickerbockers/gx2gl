; $MODE = "UniformRegister"
; $NUM_SPI_PS_INPUT_CNTL = 2
; $SPI_PS_INPUT_CNTL[0].SEMANTIC = 0
; $SPI_PS_INPUT_CNTL[1].SEMANTIC = 1

; 00 TEX: ADDR(32) CNT(1) VALID_PIX
;   0      SAMPLE          R1.xyzw, R0.xy0x, t0, s0
00 EXP_DONE: PIX0, R0.xyzw ; TODO SHADEr
END_OF_PROGRAM
